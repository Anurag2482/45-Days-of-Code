//WAP to create File and Write Some Data.
#include<stdio.h>
int main()
{
  FILE *fp;
  char ch;
  fp=fopen("H:\\Corona.txt","w");

  printf("Enter some data:\n");

  while((ch=getchar())!=EOF)
  {
      putc(ch,fp);
  }

  fclose(fp);
  return 0;
}
